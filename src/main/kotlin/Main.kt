import view.IndigoApplication

fun main() {
    // Create an instance of the IndigoApplication
    IndigoApplication().show()
    // Print a message indicating that the application has ended
    println("Application ended. Goodbye")
}